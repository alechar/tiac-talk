# Neon PostgreSQL (serverless)
spring.datasource.url=jdbc:postgresql://ep-flat-cell-alpevc4b-pooler.c-3.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_hDHpwzuna39A
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

# Disable H2 console when using Neon
spring.h2.console.enabled=false

# Disable data.sql (Neon DB is pre-seeded)
spring.jpa.defer-datasource-initialization=false
spring.sql.init.mode=never
